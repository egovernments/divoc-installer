apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: analytics_feed
  namespace: divoc
spec:
  maxReplicas: 2
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: analytics_feed
  targetCPUUtilizationPercentage: 70